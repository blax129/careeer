{
  "name": "fifa-careers-replica",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/verify-emailjs-build.js",
    "preview": "vite preview",
    "fetch-jobs": "curl -sL https://jobs.fifa.com/postings.json -o public/postings.json"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1"
  }
}
